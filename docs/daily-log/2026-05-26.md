# Daily handoff — 2026-05-26

Days remaining to Sundance: 24
Branch: develop
Final HEAD: 662b24e

---

## What shipped today (9 commits)

    662b24e  docs(phase8): Round 4 Warehouse walkthrough
    6792b69  fix(polish): B10 + B13 + D4 from pre-Sundance audit
    aa2e243  feat(ops): Slesh polling pre-flight smoke test (A6)
    561b946  docs: orphan users + QR onboarding spec
    605014e  docs(phase8): Rounds 1-3 walkthrough findings
    e9b6456  docs(training): de-dup + accurate framing
    7d4c8d7  fix(dashboard+auth): lastPath TTL + lifecycle filter

    + from 2026-05-25:
    c8b9218  feat(slesh): DATA LOSS warning + API quirks
    9a71f76  docs(slesh): shop-to-bar mapping verified

---

## Critical Sundance bugs fixed today

1. Slesh chunk truncation — adapter logs DATA LOSS warning when
   a time chunk has > 100 orders. Live API quirks documented.
2. Stale lastPath — Owner login no longer redirects to a stale
   page after restart. 30-min TTL guard.
3. Inflated alert counter — was 117, DB truth was 3. Fixed by
   carrying lifecycle_state through the legacy-shape adapter
   and filtering on it in counter + list + bar view.
4. Cheat sheet errors — owner sheet had duplicate sections 1-5;
   manager sheets used overstated 'silent investigation' wording.
5. Bilingual disclaimer (B10) and lowercase role badges (B13).
6. /pos/freshness extended with experience_id + last_seen_ts (D4).

---

## Walkthrough scoreboard

| Role        | Status        | Findings        |
|-------------|---------------|-----------------|
| Owner       | SUNDANCE-SAFE | 3 (2 fixed)     |
| Manager     | SUNDANCE-SAFE | 4 verifications |
| Bartender   | SUNDANCE-SAFE | 4 (1 P3-1)      |
| Warehouse   | SUNDANCE-SAFE | 5 (all S3/S4)   |
| Stress (R5) | PENDING       | needs devices   |
| Lifecycle   | PENDING       | needs devices   |

Zero S1 and zero S2 bugs in any single-role surface.

---

## Operator tools shipped

- ./scripts/slesh-poll-smoke.sh — pre-flight check for the Slesh
  polling pipeline. ~1.4s, exits 0 on green, 1 on red.
  Use on Sundance day at T-2h, then 10 min into event.

---

## What needs Omar's input before Sundance

1. Five orphan users with bar_id=NULL need bar assignments:
     manager.focacceria      likely Focacceria bar
     manager.malandrino      likely Malandrino bar
     bartender.giulia        unclear
     bartender.paolo         unclear
     bartender.sofia         unclear

2. Wine Station bar has NULL slesh_negozio_id — confirm real
   non-POS bar or deactivate.

3. Warehouse inventory shows 1 SKU only — by design or seed gap?

4. P3-1 design call — bartender dashboard chat: read-only or
   inline send box?

---

## Tomorrow's plan (2026-05-27)

Morning (solo):
- Send Omar Q1 + Q2 (single short Italian message). Wait for reply.
- Verify Rounds 5 + 6 are NOT possible solo (need >=2 phones).

Multi-device session:
- Multiple managers + bartenders connected over WiFi
- Real-time chat under load (10+ messages in 10 seconds)
- Backend down/up recovery
- Network drop on a phone, reconnect handling
- Session expiry on phone, re-login flow
- Concurrent acknowledge of same alert from 2 clients

Find at least 2 spare phones for bartender/manager devices.

After Omar replies:
- Fix orphan users (one SQL UPDATE per user with correct bar_id)
- Decide on Wine Station
- Pick P3-1 path

---

## Stack bring-up sequence (if laptop closed)

1. Open Docker Desktop (it doesn't auto-start)
2. Wait for Redis + MinIO to go green
3. Tab 1: cd backend && source venv/bin/activate &&
          uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
4. Tab 2: cd backend && source venv/bin/activate &&
          arq app.workers.scheduler.WorkerSettings
5. Tab 3: cd frontend && npm run dev

---

## Sign-off

9 commits, 6 critical bugs fixed, 4 roles verified Sundance-safe,
21 walkthrough findings logged, 1 operator tool shipped, 1
post-event feature scoped.

Sundance is 24 days out. We are on track.
