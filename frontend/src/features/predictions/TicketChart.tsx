/**
 * TicketChart — line chart of ticket scan velocity over time with demand forecast overlay.
 * Wraps the shared LineChart component with prediction-specific data shaping.
 */
export function TicketChart() {
  // TODO: compose shared/charts/LineChart with ticket scan data + prediction overlay
  return (
    <div className="bg-white border border-[#E2E8F0] rounded-lg p-4 h-64 flex items-center justify-center">
      <p className="text-[#4A5568] text-sm">Ticket scan chart coming soon</p>
    </div>
  )
}
