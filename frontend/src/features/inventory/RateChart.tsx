/**
 * RateChart — line chart of stock consumption rate over time for a single SKU.
 * Wraps the shared LineChart component with inventory-specific data shaping.
 */
export function RateChart() {
  // TODO: accept timeseries data and render via shared/charts/LineChart
  return (
    <div className="bg-white border border-[#E2E8F0] rounded-lg p-4 h-48 flex items-center justify-center">
      <p className="text-[#4A5568] text-sm">Consumption rate chart coming soon</p>
    </div>
  )
}
