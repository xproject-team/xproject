/**
 * SettingsPage — user profile, preferences, and admin configuration.
 * Thin page: layout + composition only, no business logic.
 */
export default function SettingsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-[#1A202C] mb-4">Settings</h1>
      {/* TODO: compose ProfileForm, NotificationPreferences, AdminSection */}
    </div>
  )
}
